package org.firstinspires.ftc.teamcode.usbserial;

public class SerialCommand {

    /*
    private final byte[] byteArray;

    public SerialCommand(CommandType commandType){
        this(commandType, 0, 0, 0);
    }

    public SerialCommand(CommandType commandType, int controllerNumer) {
        this(commandType, controllerNumer, 0, 0);
    }

    public SerialCommand(CommandType commandType, int controllerNumber, int motorOrServoNumber) {
        this(commandType, controllerNumber, motorOrServoNumber, 0);
    }

    public SerialCommand(CommandType commandType, int controllerNumber, int motorOrServoNumber, int commandValue) {
        byteArray = new byte[]{(byte) commandType.ordinal(), (byte) controllerNumber, (byte) motorOrServoNumber, (byte) commandValue};
    }

    public byte[] getByteArray(){
        return byteArray;
    }

    public enum CommandType {
        CMD_HEARTBEAT,
        CMD_SETMOTORPOWER,
        CMD_READENCODER,
        CMD_RESETENCODER,
        CMD_SETMOTORDIRECTION,
        CMD_SETMOTORZEROPOWERBEHAVIOUR,
        CMD_READMOTORCURRENT,
        CMD_SETSERVOPOSITION,
        CMD_READDISTANCE
    }

     */
}
